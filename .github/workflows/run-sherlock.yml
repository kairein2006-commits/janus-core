name: Run Sherlock OSINT

on:
  repository_dispatch:
    types: [run-sherlock]

jobs:
  osint:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Sherlock
        run: |
          python sherlock/sherlock.py "${{ github.event.client_payload.username }}" --print-found --output result.json || true
          # Посмотрим список файлов в папке, чтобы найти, куда он записался
          ls -la
          
      - name: Commit and push results
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto: Update OSINT results for ${{ github.event.client_payload.username }}"
          file_pattern: result.json
